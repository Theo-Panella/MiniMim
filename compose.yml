services:
  api:
    build: .
    ports:
      - 8000:8000

    command: gunicorn --bind 0.0.0.0:8000 api:app

  agent:
    build: .
    volumes:
      - Log_paths:/app/Log_paths:ro

    command: echo "Agente está de pé, QUE COMECE A CAÇA DE LOGS!"

volumes:
  Log_paths: